# API Configuration
# Leave empty to use default (/api with proxy)
# VITE_API_URL=http://localhost:3001/api

# Data Mode
# static: Use curated currere family data (default)
# api: Use full SQLite database via API server
VITE_DATA_MODE=static
